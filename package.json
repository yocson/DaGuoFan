{
  "name": "DaGuoFan",
  "version": "1.0.0",
  "description": "fanfou desktop application",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "packager": "electron-packager ./ DaGuoFan --platform=win32 --arch=x64 --out ./outApp --overwrite --ignore=./node_modules/* --ignore=./outApp --icon=./icons/ic.ico"
  },
  "author": "yocson",
  "license": "MIT",
  "dependencies": {
    "electron": "^2.0.2"
  },
  "devDependencies": {
    "electron-packager": "^12.1.0"
  }
}
